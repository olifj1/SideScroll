# SideScroll v0.2.56

- Cropped the new fallen-tree texture to its actual visible alpha bounds, removing the ~10% empty area beneath/around the painted asset that made it look raised and undersized.
- Kept the authored fallen-tree world transform and collision unchanged.
- Kept the synthetic fallen-tree shadow disabled.
- Puzzle image loading now preserves configured authored aspect ratios instead of letting the asynchronous source-image ratio overwrite them.
- Includes the v0.2.55 smaller/lighter movement controls.
